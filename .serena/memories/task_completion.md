# Task Completion

- After substantive code or asset integration changes, run `pnpm build`.
- If frontend layout is changed, inspect the affected page in a browser when feasible.
- Keep diffs small. Do not add production dependencies or broad refactors unless explicitly requested.
